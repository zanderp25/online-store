// TODO:
// - addCartItem(id) function that adds the item with the given id to the cart, storing the id or incrementing the 
//   quantity in localStorage
// - loadCart() function that loads the cart from localStorage and displays the items in the #cart-items element
// - removeCartItem(id) function that removes the item with the given id from the cart and updates localStorage
// - clearCart() function that removes all items from the cart and updates localStorage